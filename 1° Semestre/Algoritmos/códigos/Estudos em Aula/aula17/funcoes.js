// preciso falar com o Gus
    //  criar um readme.md

// Meu papel, criar um modal para as telas de cadastrar um usuário
    // botão cancelar
    // botão próximo
    // botão cadastrar

// preciso falar com o anderson também, vou precisar de todos os documentos para o diretório "Documentação"
    // provavelmente será apenas o diagrama de negócio


function adicionarNoFinal(lista, elemento){
    let nova_lista = [];

    for(let i = 0; i < lista.length; i++){
        nova_lista[i] = lista[i];
    };

    nova_lista[length] = elemento;

    return nova_lista;
};
